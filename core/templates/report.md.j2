# Suasiv — Communication Coaching Report

**Video:** {{ metadata.get('video', 'N/A') }}
**Duration:** {{ "%.1f"|format(metadata.get('duration', 0)) }}s
**Analyzers:** {{ metadata.get('analyzers_run', [])|join(', ') }}

---

## Summary Scores

| Dimension | Score |
|-----------|-------|
{% for dim, score in scores.items() %}
| {{ dim | replace('_', ' ') | title }} | {{ "%.0f"|format(score * 100) }}% |
{% endfor %}

---

## Coaching Narrative

{{ narrative }}

---

## Key Moments

{% for moment in moments %}
### {{ loop.index }}. [{{ "%.1f"|format(moment.start) }}s - {{ "%.1f"|format(moment.end) }}s] {{ moment.type | title }} Moment

{{ moment.description }}

{% endfor %}

---

## Signal Timeline

| Time | Analyzer | Type | Value |
|------|----------|------|-------|
{% for signal in signals[:30] %}
| {{ "%.1f"|format(signal.start) }}s | {{ signal.analyzer }} | {{ signal.type }} | {{ signal.value if signal.value is not none else '' }} |
{% endfor %}
{% if signals|length > 30 %}
| ... | *{{ signals|length - 30 }} more signals* | | |
{% endif %}

---

*Generated by Suasiv v0.1.0 — signals, not emotions.*
